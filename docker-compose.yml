version: "3.9"

services:
  cdk-avail-da-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cdk-avail-da-server
    env_file:
      - .env
    ports:
      - "8080:8080"
    restart: unless-stopped
    volumes:
      - .env:/app/.env